# Para iniciar a aplicação como desenvolvimento
``npm run dev

# Para iniciar o docker
``docker-compose up -d

# Para gerar os arquivos da api
`` npm install next-cli -g
``next g res atracoes --no-spec